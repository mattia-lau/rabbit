{
  "name": "workspace",
  "type": "module",
  "scripts": {
    "build": "tsc -p ./tsconfig.build.json --outdir dist"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.1.6",
    "typescript": "^5.5.3"
  }
}
